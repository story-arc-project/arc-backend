"""
config.py
============================================================================
  Gemini(Google AI Studio) 연결 설정 + 모델 선택
============================================================================

이 파일은 "API 키 입력 / 모델 선택"만 따로 모아 둔 곳입니다.
  - GOOGLE_AI_STUDIO_API_KEY : Google AI Studio에서 발급받은 API 키
  - GEMINI_MODEL_NAME        : 사용할 모델 이름

주의)
  * 코드를 배포하거나 커밋할 때 실제 키를 넣지 마세요.
  * 가장 안전한 방법은 환경변수(GOOGLE_AI_STUDIO_API_KEY)를 사용하는 것이며,
    환경변수가 없으면 아래 상수값을 사용합니다.
"""

import os


# ==========================================================================
#  ↓↓↓  [ 여기에 API 키를 입력하세요 / ENTER YOUR API KEY HERE ]  ↓↓↓
# ==========================================================================
#  Google AI Studio (https://aistudio.google.com/apikey) 에서 발급.
#  실행 시 직접 채워 넣으세요. (공란으로 둔 상태)
GOOGLE_AI_STUDIO_API_KEY = ""          # 예: "AIza...."

#  사용할 모델 (요청하신 gemini-2.5-flash 로 고정)
GEMINI_MODEL_NAME = "gemini-2.5-flash"
# ==========================================================================
#  ↑↑↑  [ API 키 / 모델 선택 구역 끝 ]  ↑↑↑
# ==========================================================================


# --------------------------------------------------------------------------
#  ★ 중요: thinking(추론)과 max_output_tokens 의 관계
# --------------------------------------------------------------------------
#  gemini-2.5-flash 는 '추론(thinking)'이 기본 활성화되어 있고,
#  추론에 쓰인 토큰도 max_output_tokens 예산을 함께 소모합니다.
#  → 예산이 작으면(과거 4096) 추론이 예산 대부분을 먹어 본문이 몇 백 자에서
#    잘려 나갔습니다. (예: 500자 제한 문항인데 311자에서 끊기는 현상)
#
#  한때 thinking_budget=0 으로 추론 자체를 꺼서 이 문제를 막았지만, 근거
#  검증(사실 확인)이나 액션플랜의 우선순위 진단처럼 실제 '판단'이 필요한
#  단계에서는 추론을 꺼버리면 품질이 떨어질 수 있다는 우려가 있어 전면
#  비활성화를 그만두기로 했습니다.
#
#  대신 max_output_tokens 를 넉넉하게(16384) 잡아 추론 토큰과 본문 토큰이
#  같은 예산 안에서 함께 여유 있게 돌아가도록 하고, 그래도 잘리는 경우를
#  대비해 gemini_client.generate_complete() 의 자동 이어쓰기로 이중 안전
#  장치를 둡니다. thinking 은 모델 기본값(동적 추론)에 맡깁니다.
# --------------------------------------------------------------------------

#  생성 파라미터 (환각 방지를 위해 낮은 temperature 를 기본값으로 사용)
GENERATION_CONFIG = {
    "temperature": 0.25,     # 창의성 최소화 → 사실 충실도 ↑
    "top_p": 0.9,
    "top_k": 40,
    "max_output_tokens": 16384,  # 추론 + 본문이 함께 들어가도록 넉넉하게
}

#  근거 검증(사실 확인) 단계에서 쓰는 더 엄격한 설정
VERIFICATION_CONFIG = {
    "temperature": 0.0,      # 판정은 결정적으로
    "top_p": 1.0,
    "max_output_tokens": 16384,
}

#  액션플랜 / 작성가이드처럼 '길이 제한 없는 조언 텍스트'용 설정.
#  자소서 본문(글자수 제한 있음)보다 훨씬 길어질 수 있어 예산을 더 크게 잡는다.
#  (액션플랜이 '리스크 관리, 투자 전략 최적...' 처럼 끊기던 문제의 직접 원인)
GUIDE_CONFIG = {
    "temperature": 0.3,
    "top_p": 0.9,
    "top_k": 40,
    "max_output_tokens": 16384,
}

#  응답이 토큰 한도로 잘렸을 때 '이어쓰기'를 최대 몇 번까지 시도할지
MAX_CONTINUATIONS = 2


def resolve_api_key() -> str:
    """
    실제로 사용할 API 키를 반환한다.

    우선순위:
      1) 환경변수 GOOGLE_AI_STUDIO_API_KEY
      2) 이 파일 상단의 GOOGLE_AI_STUDIO_API_KEY 상수
    """
    return os.environ.get("GOOGLE_AI_STUDIO_API_KEY", "") or GOOGLE_AI_STUDIO_API_KEY
